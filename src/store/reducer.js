// import * as types from './constants';

const initialState = {
  web3: {},
};

export default function (state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
